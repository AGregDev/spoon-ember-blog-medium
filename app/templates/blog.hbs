<div class="flex flex-col w-full justify-center items-center py-16">
    <BlogNavigator @blogs={{@model.blogs}} @index={{@model.index}} />
    <h1 class="text-blue-950 text-3xl font-extrabold text-center">{{@model.blog.title}}</h1>
    <div class="flex flex-col gap-4 w-full items-center text-lg py-10 text-justify px-24">
        {{{@model.blog.content}}}
    </div>
    <BlogNavigator @blogs={{@model.blogs}} @index={{@model.index}} />
</div>